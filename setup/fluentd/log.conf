<source>
  @type forward
</source>

<match *>
  @type file
  path /var/log/fluent/${container_name}/log_%Y%m%d_%H%M
  append true

  <buffer time, container_name>
    @type file
    timekey      10m
    timekey_wait 1m
  </buffer>

  <format>
    @type json
  </format>
</match>
